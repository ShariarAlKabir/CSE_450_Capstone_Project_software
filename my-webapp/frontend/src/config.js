// Base URL for the Label Inspection API.
// Uses the host that served the page, so the app works from any device on the network.
export const API_BASE_URL = `http://${window.location.hostname}:8000`;
